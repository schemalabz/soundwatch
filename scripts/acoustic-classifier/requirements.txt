# Mainstream scientific + audio stack. Versions are lower bounds known to work
# (tested on the pinned set in the comment). Python 3.10+ recommended.
numpy>=1.26        # tested 2.4.6
scipy>=1.11        # tested 1.18.0  (resample_poly)
scikit-learn>=1.3  # tested 1.9.0   (RandomForest, metrics)
pandas>=2.0        # tested 3.0.3   (metadata + master table)
matplotlib>=3.7    # tested 3.11.0  (confusion PNGs)
soundfile>=0.12    # tested 0.14.0  (primary audio loader)
librosa>=0.10      # tested 0.11.0  (fallback loader / resampling for odd formats)
pytest>=7.0        # tested 8.x     (emulator faithfulness tests)
